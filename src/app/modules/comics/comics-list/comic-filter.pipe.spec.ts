import { ComicFilterPipe } from './comic-filter.pipe';

describe('ComicFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ComicFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
